fun main(){
    var y: Int = 10;
    while (y<=100){
        println(y);
        y = y*2;
    }
    println("Final value of y is $y") // the output works differently in this one but this uses less lines
}